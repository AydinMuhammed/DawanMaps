<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Nom du centre -->
        <TextView
            android:id="@+id/detail_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            tools:text="Paris" />

        <!-- Adresse complète -->
        <TextView
            android:id="@+id/detail_address"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:lineSpacingExtra="4dp"
            android:textColor="@color/black"
            android:textSize="16sp"
            tools:text="11 rue Antoine Bourdelle" />

        <!-- Ville + code postal -->
        <TextView
            android:id="@+id/detail_city"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/black"
            android:textSize="16sp"
            tools:text="75015 Paris" />

        <!-- Pays -->
        <TextView
            android:id="@+id/detail_country"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            tools:text="France" />

        <!-- Info complémentaire -->
        <TextView
            android:id="@+id/detail_further_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textSize="14sp"
            android:textStyle="italic"
            tools:text="dans le 15e arrondissement" />

        <!-- Badge : locaux -->
        <TextView
            android:id="@+id/detail_office"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/badge_background"
            android:padding="8dp"
            android:textSize="13sp"
            tools:text="Centre avec locaux" />

        <!-- Badge : CPF -->
        <TextView
            android:id="@+id/detail_pmi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/badge_background"
            android:padding="8dp"
            android:textSize="13sp"
            tools:text="Éligible CPF" />
    </LinearLayout>
</ScrollView>